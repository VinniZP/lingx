# Lingx Docker Compose Configuration
# Start the entire stack with: docker-compose up -d

services:
  postgres:
    image: postgres:16-alpine
    container_name: lingx-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-lingx}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lingx}
      POSTGRES_DB: ${POSTGRES_DB:-lingx}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-lingx} -d ${POSTGRES_DB:-lingx}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - lingx-network

  redis:
    image: redis:7-alpine
    container_name: lingx-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - lingx-network

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: lingx-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://${POSTGRES_USER:-lingx}:${POSTGRES_PASSWORD:-lingx}@postgres:5432/${POSTGRES_DB:-lingx}
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      PORT: 3001
    ports:
      - "${API_PORT:-3001}:3001"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 3s
      retries: 3
    networks:
      - lingx-network

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3001}
    container_name: lingx-web
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3001}
    ports:
      - "${WEB_PORT:-3000}:3000"
    networks:
      - lingx-network

volumes:
  postgres_data:
  redis_data:

networks:
  lingx-network:
    driver: bridge
