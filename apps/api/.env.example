# Development database
DATABASE_URL="postgresql://user:password@localhost:5432/lingx?schema=public"

# Test database (uses separate 'test' schema for isolation)
TEST_DATABASE_URL="postgresql://user:password@localhost:5432/lingx?schema=test"

# AI Quality Evaluation
# LOG_AI_PROMPTS=true   # Log prompt summary (key, locales, related keys count)
# LOG_AI_PROMPTS=full   # Log full prompt and response text
